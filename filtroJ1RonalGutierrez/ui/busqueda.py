import menu as m
import json
import os

def busquedaPrincipal():
    while True:
        try:
            m.sistemaGestor()
            opcPrincipal = int(input('Ingrese el numero de la accion a realizar\n->: '))
            break
        except ValueError:
            print('Dato invalido, intente nuevamente')
            os.system('cls')
        if (opcPrincipal == 1):
            #administrador de generos
        elif (opcPrincipal == 1):
            #administrador de generos
        elif (opcPrincipal == 1):
            #administrador de generos
        elif (opcPrincipal == 1):
            #administrador de generos
        elif (opcPrincipal == 1):
            #administrador de generos
        elif (opcPrincipal == 1):
            #administrador de generos
busquedaPrincipal()